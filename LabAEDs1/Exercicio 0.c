#include <stdio.h>
#include <stdlib.h>

int main(){
    int a, b, r;
    printf("Digite os dois numeros: ");
    scanf("%d %d",&a,&b);

    r = a+b;

    printf("Resultado = %d",r);

return 0;
}
